# Server
PORT=3000
EDEN_CALL_MODE=dry_run
# Optional: override all shelter calls to this number for testing (e.g. +14805483012)
# EDEN_TEST_SHELTER_PHONE=
# Max shelters to call per intake search (default 1 to avoid overwhelming)
EDEN_MAX_CALLS_PER_INTAKE=1

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eden_db
DB_USER=postgres
DB_PASSWORD=postgres

# Twilio (required for live mode)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_STATUS_CALLBACK_URL=
TWILIO_WARM_TRANSFER_STATUS_CALLBACK_URL=

# Optional: AI-powered scripts
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# If unset/invalid, Eden will use fallback scripts and mark generated_script_source=fallback

# Optional: ElevenLabs voice (Option 1 TTS via Twilio <Play>)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL
ELEVENLABS_MODEL_ID=eleven_turbo_v2_5
# Required for live mode voice. If unset/invalid, live attempts fail with a config error.

# Optional: ElevenLabs Conversational AI Agent (replaces TTS+Gather for better voice)
# When set, calls use the agent for real-time conversation. No Twilio number needed in ElevenLabs.
ELEVENLABS_AGENT_ID=

# Optional: Call recording
ENABLE_CALL_RECORDING=false
NGROK_URL=
# Required for live ElevenLabs playback URLs reachable by Twilio

# Safety
EDEN_REQUIRE_ESCALATION_FOR_LIVE=true
EDEN_NO_CALLBACK_NUMBERS=

# Persistence
EDEN_PERSISTENCE_ENABLED=true
